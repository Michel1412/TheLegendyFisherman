// priority: 10
// Modelo 3D herdado de fishing_net.json (textura kubejs:item/fishing_net).

StartupEvents.registry('item', function (event) {
  event.create('hook').displayName('Anzol improvisado')
})
